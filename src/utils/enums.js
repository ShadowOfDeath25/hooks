export const DeliveryClassification = Object.freeze({
    SUCCESS: 'success',
    RETRIABLE: 'retriable',
    TERMINAL: 'terminal'
});

export const DeliveryStatus = Object.freeze({
    PENDING: 'pending',
    SUCCESS: 'success',
    FAILED: 'failed'
});
